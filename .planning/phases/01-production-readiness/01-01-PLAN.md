---
phase: 01-production-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Okozukai.Api/Program.cs
  - src/Okozukai.ServiceDefaults/Extensions.cs
  - .env.example
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03, PROD-04, PROD-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Goal-backward (ROADMAP success criteria 1-5, D-12) ---
    - "Starting the API with ASPNETCORE_ENVIRONMENT=Production against an empty database applies all six migrations and leaves a schema containing the Journals, Tags, Transactions and TransactionTags tables (PROD-01, SC-1)."
    - "After a Production start, the Journals, Tags and Transactions tables each contain zero rows — no development seed data is written (PROD-02, SC-2)."
    - "A plain-HTTP request to the API running in Production returns the endpoint's own response and never a 30x redirect to an https:// location, so no redirect loop can form (PROD-03, SC-3)."
    - "GET /health and GET /alive each return HTTP 200 with body Healthy when ASPNETCORE_ENVIRONMENT=Production (PROD-04, SC-4)."
    - "The API starts with its database connection string supplied only as the ConnectionStrings__okozukai environment variable, with no user-secrets store consulted (PROD-05, SC-5)."
    - "/health aggregates the PostgreSQL connectivity check while /alive reports only the process-level self check — the readiness/liveness contract Phase 2 consumes (D-12)."
    # --- Edge lift: PROD-01 ---
    - "Starting the API a second time against an already-migrated database applies zero further migrations, leaves __EFMigrationsHistory at six rows, and still reaches a serving state (PROD-01 adjacency)."
    - "Against a connection string naming a database that does not yet exist, the 3D000 fallback creates the database and the six migrations then apply, on a role holding CREATEDB (PROD-01 empty, D-03)."
    - "__EFMigrationsHistory ordered by MigrationId ascending lists the six migrations in timestamp order, beginning 20260219111426_InitialCreate and ending 20260223000000_AddTransactionCreatedAt (PROD-01 ordering)."
    - statement: "A startup interrupted part-way through migration leaves no partially-applied migration — the next start resumes from the last recorded __EFMigrationsHistory row and reaches a working schema. Concurrent migration is out of scope because single-instance is a locked milestone constraint (PROD-01 concurrency)."
      verification: backstop
    # --- Edge lift: PROD-03 / PROD-04 ---
    - "Ten concurrent plain-HTTP requests spanning /health and /api/journals all return a non-3xx status and none carries a Location response header (PROD-03 concurrency)."
    - "Ten concurrent GET /health requests all return HTTP 200 Healthy — none returns 503 or a DbContext-reentrancy failure from the shared scoped DbContext (PROD-04 concurrency)."
    # --- Edge lift: PROD-05 ---
    - "Okozukai.Api.csproj declares no user-secrets identifier and neither appsettings.json nor appsettings.Development.json declares a ConnectionStrings section, so the process environment is the only source the connection string can come from (PROD-05 adjacency)."
    - "Starting with ConnectionStrings__okozukai unset, empty, or whitespace-only terminates the process before any migration attempt, with an error naming ConnectionStrings__okozukai (PROD-05 empty, D-14)."
    - "Program.cs registers no additional and no reordered configuration sources, so the framework's last-wins provider order holds and an environment-supplied value overrides any file-supplied one (PROD-05 ordering)."
    - statement: "A connection-string password containing Npgsql keyword-value delimiters (semicolon or equals) is supplied using Npgsql's quoting rules; .env.example shows only the plain unquoted form and the real value is authored at deploy time in Phase 2 (PROD-05 encoding)."
      verification: backstop
    - "The connection string is read exactly once, during host construction and before AddNpgsqlDbContext, and is never re-read or mutated while the app serves traffic (PROD-05 concurrency)."

  prohibitions:
    - "The API must not serve ledger data against a partially-applied schema — a migration failure must terminate the process rather than fall through to serving requests."
    - "Startup must never destroy existing ledger data in order to reconcile the schema — no startup path may drop, truncate, or recreate the Journals, Tags, Transactions or TransactionTags tables to make the database match the model."
    - "Sample records must never be written into a database that holds real user data, in any environment — the seed path deletes all existing journals and tags before writing, so contamination is destructive rather than merely cosmetic."

  artifacts:
    - ".env.example at the repository root, naming ASPNETCORE_ENVIRONMENT, ASPNETCORE_URLS and ConnectionStrings__okozukai"
    - "src/Okozukai.Api/Program.cs with the migration call at top level and the connection-string guard placed before AddNpgsqlDbContext"
    - "src/Okozukai.ServiceDefaults/Extensions.cs with MapDefaultEndpoints mapping both health routes in every environment"

  key_links:
    - "ConnectionStrings__okozukai (process env) -> IConfiguration key ConnectionStrings:okozukai (double-underscore mapping) -> builder.AddNpgsqlDbContext<OkozukaiDbContext>(\"okozukai\") — renaming either side silently yields a null connection string"
    - "Top-level app.Services.ApplyDatabaseMigrations() in Program.cs -> MigrationExtensions retry loop -> __EFMigrationsHistory — re-gating this call behind an environment check reopens PROD-01"
    - "app.MapDefaultEndpoints() at Program.cs:35 -> /health and /alive route registration in ServiceDefaults — reinstating the environment gate reopens PROD-04"
    - "AddNpgsqlDbContext's implicit untagged DbContextHealthCheck -> included by /health, excluded by /alive — tagging it live, or adding a duplicate health-check package, breaks the Phase 2 readiness/liveness contract (D-10, D-12)"
---

<objective>
Make Okozukai's API behave correctly under `ASPNETCORE_ENVIRONMENT=Production` — the mode it
will run in on the homelab — instead of only under Development. Closes PROD-01 through
PROD-05.

The work is overwhelmingly *subtractive*: every mechanism the five requirements need already
exists in production-grade form (the migration retry loop with its `3D000` fallback, the
readiness/liveness health-check split, the environment-variable configuration provider). What
is wrong is that three of them sit behind `IsDevelopment()` gates or behind middleware that
makes no sense in this topology. Remove the gates (D-01, D-04, D-06, D-09), add one fail-fast
configuration guard (D-14), and commit one deployment-contract file (D-16).

Purpose: nothing in Phase 2 (packaging) or Phase 3 (Tailnet access) is meaningfully testable
until the app is correct in Production mode. STATE.md names this the milestone's hardest
blocker.

Output: a Production-capable `Program.cs`, an ungated `MapDefaultEndpoints`, a committed
`.env.example`, and recorded evidence from an actual Production boot against an empty scratch
database (D-05).
</objective>

<execution_context>
@/Users/archer.chang/workspace/Okozukai/.claude/gsd-core/workflows/execute-plan.md
@/Users/archer.chang/workspace/Okozukai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-production-readiness/01-CONTEXT.md
@.planning/phases/01-production-readiness/01-RESEARCH.md
@.planning/phases/01-production-readiness/01-PATTERNS.md
@AGENTS.md
</context>

## Artifacts this phase produces

This phase is largely subtractive, so this list is deliberately short. Everything below is
**new to this repository** and must be excluded from drift/source-grounding verification —
none of it exists at plan time.

| Artifact | Kind | Location | Introduced by |
|----------|------|----------|---------------|
| `.env.example` | new file | repository root (sibling to `.gitignore`, `README.md`, `src/`) | Task 3 (D-16) |
| `ASPNETCORE_ENVIRONMENT` | env-var contract entry (new to this repo's committed config surface) | `.env.example` | Task 3 (D-08) |
| `ASPNETCORE_URLS` | env-var contract entry (new to this repo's committed config surface) | `.env.example` | Task 3 (D-08) |
| `ConnectionStrings__okozukai` | env-var contract entry (new to this repo's committed config surface) | `.env.example`, read by `Program.cs` | Tasks 2 and 3 (D-13) |
| `connectionString` | new local variable (top-level statement) | `src/Okozukai.Api/Program.cs`, immediately before `builder.AddNpgsqlDbContext` | Task 2 (D-14) |

**No new public type, method, class, or package reference is introduced.** D-14's guard is an
inline top-level statement following the file's existing registration style (PATTERNS.md style
analog 2) — there is deliberately no `AddConfigurationValidation()`-style helper, because no
such helper exists anywhere in the codebase to mimic. D-10 explicitly forbids adding a package
reference for a database health check.

**Routes newly reachable outside Development** (existing constants, not new symbols):
`/health` and `/alive`, from the `HealthEndpointPath` / `AlivenessEndpointPath` constants at
`src/Okozukai.ServiceDefaults/Extensions.cs:18-19`.

<assumption_delta_decision>
**Detector:** `assumption-delta.cjs` over CONTEXT.md + RESEARCH.md returned `detected: true`.
Most surfaced signals are lexical noise (`also`, `alternative`, `fallback`), but one genuine
transition is present and is in fact the entire point of the phase.

- **Noun that is now primary:** *hosting environment*. The application previously assumed a
  single environment (`Development`) — startup behaviour, health endpoints, and configuration
  sourcing were all written against it. It must now be correct across a set of environments
  (`Development` and `Production` at minimum). Singular → plural.
- **Decision: `promote`.** The general representation becomes primary: startup behaviour is
  driven by *configuration* (environment variables) and is identical in every environment.
  The specific representation (`Development`) is demoted to exactly one narrow
  responsibility — gating sample-data seeding and the OpenAPI document. This is what D-01,
  D-04, D-06, D-09 and D-14 collectively do, and it is why D-01 rejects a `RUN_MIGRATIONS`
  opt-in flag: a flag would reintroduce a per-environment boot path, i.e. add-alongside.
- **Rationale:** divergent boot paths between environments are the exact defect this phase
  exists to remove. Adding alongside would preserve the defect under a new name.
- **Suggested (not required) invariant test:** an assertion that the count of environment-name
  branches in `Program.cs` is exactly one, and that the surviving branch encloses only the seed
  and OpenAPI calls. Advisory — this plan does not require it.
</assumption_delta_decision>

<capability_gate_notes>
- **API coverage gate:** detector returned `detected: true` on two false-positive signals; a
  reasoned no-integration declaration is written to
  `.planning/phases/01-production-readiness/COVERAGE.md`. No external API, SDK, or service is
  integrated by this phase.
- **Schema push gate:** evaluated honestly against the listed ORM patterns (Payload, Prisma,
  Drizzle, Supabase, TypeORM). This is EF Core, which matches none of them, and no migration
  or model file is in `files_modified` — the six existing migrations under
  `src/Okozukai.Infrastructure/Persistence/Migrations/` are untouched. Gate skipped; no
  `[BLOCKING]` push task is injected. (Applying the *existing* migrations at runtime is the
  subject of Task 1 and is verified there.)
- **Package legitimacy gate:** zero package-manager installs in this plan. RESEARCH.md's
  Package Legitimacy Audit records N/A, and D-10 forbids adding a health-check package. Task 1
  asserts no `.csproj` was modified.
</capability_gate_notes>

<tasks>

<!-- planner-discipline-allow: UseHttpsRedirection -->
<!-- planner-discipline-allow: IsDevelopment -->

<task type="tracer">
  <name>Task 1: End-to-end Production boot — one path only</name>

  <reversibility rating="costly">Implements D-06/D-07: removing HTTPS redirection rather than
  adding forwarded-header trust locks in a topology with no TLS terminator anywhere. Undoing it
  means revisiting Phase 2's entry point (whether a TLS-terminating proxy container exists at
  all) and Phase 3's Tailnet wiring, plus adding `KnownProxies`/`KnownNetworks` trust
  configuration. Flagged, not gated — no data migration and no published contract breaks, so
  this is not a one-way door.</reversibility>

  <precondition>A local PostgreSQL instance is accepting connections (`pg_isready` exits 0) and
  the current OS user can run `createdb`/`dropdb`, and `dotnet --version` reports a 10.x SDK —
  all required by D-05's Production boot verification, which plan ordering cannot
  guarantee.</precondition>

  <files>src/Okozukai.Api/Program.cs, src/Okozukai.ServiceDefaults/Extensions.cs</files>

  <read_first>
    - `src/Okozukai.Api/Program.cs` — the file being modified. Read it in full before editing;
      three separate edit sites live in it and the statement ordering around them must survive.
    - `src/Okozukai.ServiceDefaults/Extensions.cs` — the file being modified. Read at minimum
      lines 14-20 (the endpoint-path constants) and lines 100-130 (`AddDefaultHealthChecks` and
      `MapDefaultEndpoints`).
    - `.planning/phases/01-production-readiness/01-PATTERNS.md` — carries verbatim
      before/after excerpts for all three edit sites. Use them so the dedent is done by
      deleting the guard line and its closing brace, not by inverting a condition.
    - `src/Okozukai.Infrastructure/Persistence/MigrationExtensions.cs` — the retry loop,
      `IsRelational()` skip branch, `3D000` auto-create fallback, and rethrow-on-exhaustion
      that Production now depends on. Read it to confirm it needs no change (D-02, D-03).
    - `src/Okozukai.Infrastructure/Persistence/DevSeedData.cs` — read lines 14-40 only. The
      seed path calls `RemoveRange` on all tags and journals before writing, so it is
      destructive, not additive. This is why the seed call must remain gated.
  </read_first>

  <action>
Make the three edits below, then prove the Production path end-to-end. Do not add anything
this task does not name.

**Edit A — relocate the migration call out of the environment guard (D-01, D-04; PROD-01,
PROD-02).** In `src/Okozukai.Api/Program.cs`, the block currently at lines 37-44 gates both
`app.Services.ApplyDatabaseMigrations();` and `app.Services.SeedDevelopmentData();` behind
`if (app.Environment.IsDevelopment())`. Pull `app.Services.ApplyDatabaseMigrations();` out of
that block so it is an unindented top-level statement executing in every environment. Leave
`app.Services.SeedDevelopmentData();` and `app.MapOpenApi();` inside the guard, still indented.
Update the guard's `Console.WriteLine` message so it no longer claims to be attempting a
database migration — the seed is now its only remaining subject. The relocated call keeps the
`app.Services.` receiver (it is an `IServiceProvider` extension method), and the whole block
stays between `app.MapDefaultEndpoints();` (line 35) and `app.UseExceptionHandler();`
(line 46) — do not reorder relative to those two statements.

Per D-01 do **not** introduce a `RUN_MIGRATIONS` opt-in flag or a separate migrator entrypoint;
both were considered and rejected because they reintroduce the divergent boot path this phase
exists to remove. Per D-02 and D-03, `MigrationExtensions.cs` is not modified at all: its
rethrow-on-exhaustion (crash the process rather than serve a half-migrated ledger) and its
`3D000` auto-create fallback both stay active in Production exactly as written.

**Edit B — delete the HTTPS-redirect middleware (D-06, D-07; PROD-03).** In the same file,
delete lines 50-53 in their entirety: the `if (!app.Environment.IsDevelopment())` wrapper and
the `app.UseHttpsRedirection();` call inside it. Do not replace it with a conditional, and do
not add forwarded-header middleware — that standard fix was explicitly rejected by D-07,
because this milestone's topology has no TLS terminator anywhere and therefore nothing will
ever send `X-Forwarded-Proto`. Adding header-trust middleware with no proxy to trust would add
header-spoofing surface for zero benefit. The surviving pipeline order is
`app.UseExceptionHandler();` then `app.UseCors();` then `app.UseAuthorization();` then
`app.MapControllers();` then `app.Run();` — otherwise untouched. Leave
`src/Okozukai.Api/Properties/launchSettings.json` alone, including its `https` profile: it is
dev-only and is ignored outside `dotnet run` with a launch profile (D-08).

**Edit C — remove the environment gate on health endpoints (D-09; PROD-04).** In
`src/Okozukai.ServiceDefaults/Extensions.cs`, `MapDefaultEndpoints` (lines 109-126) wraps both
`MapHealthChecks` calls in `if (app.Environment.IsDevelopment())`. Delete that wrapper line and
its closing brace and dedent the body, so both routes map in every environment. Also delete the
two-line security-warning comment immediately above the guard (lines 111-112, the one pointing
at `aka.ms/dotnet/aspire/healthchecks`) — it explains the gate's rationale and is stale once the
gate is gone. Keep both `MapHealthChecks` calls verbatim including the
`Predicate = r => r.Tags.Contains("live")` lambda on the aliveness endpoint and their inline
comments, and keep `return app;`. Do not touch the `HealthEndpointPath` /
`AlivenessEndpointPath` constants (lines 18-19) or `AddDefaultHealthChecks` (lines 100-107).

Per D-11, both endpoints stay on the **ordinary API port** — do not bind them to a separate
management port and do not add a `RequireHost` filter. PROD-04's "internal port" wording is
satisfied by topology rather than by app code: Phase 3's ACC-03 means the API port is never
published to the host, so these routes exist only on the internal container network. A
dedicated management port was considered and rejected as defence in depth against a threat the
Tailnet model already covers.

Per D-10, add **no** health-check package reference and **no** DI wiring for a database health
check: `builder.AddNpgsqlDbContext<OkozukaiDbContext>("okozukai")` at `Program.cs:22` already
registers an untagged PostgreSQL check, which is why `/health` covers readiness while `/alive`
(predicate-filtered to the `live` tag) covers only the process. That split is the contract
Phase 2 consumes (D-12); a duplicate check or a `live` tag on the database check would break it.

Deliberately unchanged in this task: `MigrationExtensions.cs`, `DevSeedData.cs`,
`src/Okozukai.AppHost/Program.cs` (dev-only and out of scope per D-15), every `.csproj`, and
`appsettings*.json`.
  </action>

  <acceptance_criteria>
    **Source assertions — `src/Okozukai.Api/Program.cs`**
    - `grep -cE '^app\.Services\.ApplyDatabaseMigrations\(\);' src/Okozukai.Api/Program.cs` returns `1` (unindented ⇒ top level, outside every guard).
    - `grep -cE '^    app\.Services\.SeedDevelopmentData\(\);' src/Okozukai.Api/Program.cs` returns `1` (four-space indent ⇒ still inside the environment guard).
    - `grep -vE '^[[:space:]]*//' src/Okozukai.Api/Program.cs | grep -c 'UseHttpsRedirection'` returns `0`.
    - `grep -vE '^[[:space:]]*//' src/Okozukai.Api/Program.cs | grep -c 'UseForwardedHeaders'` returns `0`.
    - `grep -vE '^[[:space:]]*//' src/Okozukai.Api/Program.cs | grep -cE 'app\.Environment\.IsDevelopment'` returns `1` (exactly one surviving environment branch — the seed/OpenAPI one).
    - `grep -vE '^[[:space:]]*//' src/Okozukai.Api/Program.cs | grep -c 'RUN_MIGRATIONS'` returns `0`.

    **Source assertions — `src/Okozukai.ServiceDefaults/Extensions.cs`** (region-scoped to `MapDefaultEndpoints`, because the same token legitimately survives elsewhere in the repo)
    - `awk '/public static WebApplication MapDefaultEndpoints/,/^    }$/' src/Okozukai.ServiceDefaults/Extensions.cs | grep -vE '^[[:space:]]*//' | grep -c 'IsDevelopment'` returns `0`.
    - The same `awk` region contains exactly two `MapHealthChecks` calls and one `Tags.Contains("live")` predicate.

    **No collateral change**
    - `git diff --name-only HEAD -- '*.csproj' 'src/Okozukai.Infrastructure/Persistence/MigrationExtensions.cs' 'src/Okozukai.Infrastructure/Persistence/DevSeedData.cs' 'src/Okozukai.AppHost/Program.cs' 'src/Okozukai.Api/Properties/launchSettings.json'` prints nothing.

    **Behaviour — Production boot against an empty scratch database (D-05)**
    - `dotnet build Okozukai.slnx -nologo` exits 0.
    - `dotnet test Okozukai.slnx --no-build -nologo` exits 0 — all existing tests still pass (they run in Development against the in-memory provider and are structurally unable to exercise the Production path; this asserts no regression, not coverage).
    - With `ASPNETCORE_ENVIRONMENT=Production`, `ASPNETCORE_URLS=http://127.0.0.1:5099` and `ConnectionStrings__okozukai` pointing at a freshly created empty database, the API reaches a serving state and `GET /health` returns `200` with body `Healthy`.
    - `GET /alive` returns `200` with body `Healthy`.
    - `GET /api/journals` over plain HTTP returns `200` with an empty `curl` `redirect_url` — no `30x`, no `Location` header.
    - `select count(*) from "__EFMigrationsHistory"` returns `6`.
    - Those six `MigrationId` values in ascending order begin with `20260219111426_InitialCreate` and end with `20260223000000_AddTransactionCreatedAt`.
    - `select count(*)` on each of `"Journals"`, `"Tags"` and `"Transactions"` returns `0`.
    - The startup log contains zero occurrences of `Seed: populating`.
    - Ten concurrent `GET /health` requests all return `200`; the sorted-unique status set is exactly `200`.
    - Ten concurrent `GET /api/journals` requests all return `200`; the sorted-unique status set is exactly `200`.
    - **Second boot (idempotence):** relaunching against the same, now-migrated database reaches `200` on `/health` and `"__EFMigrationsHistory"` still holds exactly `6` rows.
    - **`3D000` fallback (D-03):** launching against a `Database=` name that does not exist results in the database being created and all six migrations applying — `/health` returns `200` and the new database's `"__EFMigrationsHistory"` holds `6` rows.
    - Both scratch databases are dropped afterwards; the developer's normal `okozukai` database is never targeted.
  </acceptance_criteria>

  <verify>
    <automated>dotnet build Okozukai.slnx -nologo &amp;&amp; dotnet test Okozukai.slnx --no-build -nologo</automated>
    <automated>
# --- D-05: Production boot against an empty scratch database ---------------------
set -u
PORT=5099; DB=okozukai_prodcheck; MISSING=okozukai_prodcheck_missing; U=$(whoami)
LOG=/tmp/okoz-prod.log
boot () {  # $1 = database name, $2 = log file
  ASPNETCORE_ENVIRONMENT=Production ASPNETCORE_URLS=http://127.0.0.1:$PORT \
  ConnectionStrings__okozukai="Host=localhost;Port=5432;Database=$1;Username=$U" \
  dotnet run --project src/Okozukai.Api --no-build --no-launch-profile >"$2" 2>&1 &
  echo $!
}
wait_health () { for _ in $(seq 1 60); do
    [ "$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:$PORT/health)" = 200 ] &amp;&amp; return 0
    sleep 1; done; echo "TIMEOUT waiting for /health"; tail -40 "$1"; return 1; }

dropdb --if-exists "$DB"; dropdb --if-exists "$MISSING"; createdb "$DB"
PID=$(boot "$DB" "$LOG"); wait_health "$LOG" || exit 1

# endpoints, no redirect
curl -s http://127.0.0.1:$PORT/health | grep -qx Healthy || exit 1
curl -s http://127.0.0.1:$PORT/alive  | grep -qx Healthy || exit 1
test "$(curl -s -o /dev/null -w '%{http_code}|%{redirect_url}' http://127.0.0.1:$PORT/api/journals)" = '200|' || exit 1
# concurrency (PROD-03 / PROD-04 concurrency edges)
test "$(seq 10 | xargs -P10 -I_ curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:$PORT/health      | sort -u)" = 200 || exit 1
test "$(seq 10 | xargs -P10 -I_ curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:$PORT/api/journals | sort -u)" = 200 || exit 1
# schema + no seed
test "$(psql -tAd "$DB" -c 'select count(*) from "__EFMigrationsHistory"')" = 6 || exit 1
psql -tAd "$DB" -c 'select "MigrationId" from "__EFMigrationsHistory" order by "MigrationId"' | head -1 | grep -qx 20260219111426_InitialCreate || exit 1
psql -tAd "$DB" -c 'select "MigrationId" from "__EFMigrationsHistory" order by "MigrationId"' | tail -1 | grep -qx 20260223000000_AddTransactionCreatedAt || exit 1
for T in Journals Tags Transactions; do test "$(psql -tAd "$DB" -c "select count(*) from \"$T\"")" = 0 || exit 1; done
test "$(grep -c 'Seed: populating' "$LOG")" = 0 || exit 1
kill "$PID"; wait "$PID" 2>/dev/null

# second boot: migrations are idempotent (PROD-01 adjacency)
PID=$(boot "$DB" /tmp/okoz-prod2.log); wait_health /tmp/okoz-prod2.log || exit 1
test "$(psql -tAd "$DB" -c 'select count(*) from "__EFMigrationsHistory"')" = 6 || exit 1
kill "$PID"; wait "$PID" 2>/dev/null

# 3D000 auto-create fallback against a database that does not exist (D-03, PROD-01 empty)
PID=$(boot "$MISSING" /tmp/okoz-prod3.log); wait_health /tmp/okoz-prod3.log || exit 1
test "$(psql -tAd "$MISSING" -c 'select count(*) from "__EFMigrationsHistory"')" = 6 || exit 1
kill "$PID"; wait "$PID" 2>/dev/null

dropdb "$DB"; dropdb "$MISSING"; echo "PRODUCTION BOOT VERIFICATION PASSED"
    </automated>
    <human-check>
Confirm at end of phase (`human_verify_mode: end-of-phase`): read the captured startup log
from the first Production boot and confirm it shows the migration attempt and success lines
with no environment-name announcement of seeding, and that the process never printed an
HTTPS-redirect or certificate warning. This is the human read of the same run the automated
block asserts on — it does not require re-running anything.
    </human-check>
  </verify>

  <done>
The API boots under `ASPNETCORE_ENVIRONMENT=Production` with environment-variable configuration
only, applies all six migrations to a brand-new empty database (and creates the database itself
if it is absent), writes no seed data, serves `/health` and `/alive` with `200 Healthy`, and
answers plain-HTTP API requests with no redirect — all proven by a real boot, and committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fail fast when the connection string is absent</name>

  <precondition>Task 1 is committed — `app.Services.ApplyDatabaseMigrations()` executes
  unconditionally, so a missing connection string now reaches the migration retry loop in every
  environment. That is the failure mode this guard intercepts.</precondition>

  <files>src/Okozukai.Api/Program.cs</files>

  <read_first>
    - `src/Okozukai.Api/Program.cs` — the file being modified, in its post-Task-1 state. Read
      lines 1-35 to see the registration block the guard is inserted into.
    - `.planning/phases/01-production-readiness/01-PATTERNS.md` — "fail-fast connection-string
      guard" section. It supplies both style analogs: the plain-exception convention and the
      "insert as a bare top-level statement, not a new extension method" placement rule.
    - `src/Okozukai.Infrastructure/Persistence/MigrationExtensions.cs` — lines 18-58. This is
      the ten-round retry loop whose opaque Npgsql output the guard exists to pre-empt; read it
      to confirm the guard must run strictly earlier.
    - `src/Okozukai.Infrastructure/DependencyInjection.cs` — read to confirm what NOT to do:
      `AddInfrastructure()` is invoked at `Program.cs:28`, after the DbContext registration, so
      the guard must not live there.
  </read_first>

  <action>
Insert a fail-fast configuration guard as a plain top-level statement in
`src/Okozukai.Api/Program.cs`, immediately **before**
`builder.AddNpgsqlDbContext<OkozukaiDbContext>("okozukai");` (currently line 22, per D-14 and
PROD-05).

Read the value with `builder.Configuration.GetConnectionString("okozukai")` into a local named
`connectionString`. `GetConnectionString(name)` resolves the configuration key
`ConnectionStrings:okozukai`, which is exactly where ASP.NET Core's environment-variable
provider lands the `ConnectionStrings__okozukai` variable via its double-underscore-to-colon
convention — so no code is needed to *read* the environment variable, only to validate it
(D-13). If the value is null, empty, or whitespace-only (`string.IsNullOrWhiteSpace`), throw an
`InvalidOperationException` whose message names the missing key exactly as an operator would
set it — the token `ConnectionStrings__okozukai`, double underscore — and tells them to set it
as an environment variable before starting the API.

Three constraints on this guard:

1. **Placement is load-bearing.** It must execute before the DbContext registration, not inside
   `AddInfrastructure()` and not in any later helper. `AddNpgsqlDbContext` is a DI registration,
   not a connection attempt, so a missing value does not surface there — it surfaces ten retries
   later inside the migration loop as Npgsql host-resolution noise, which is precisely the
   symptom being eliminated.
2. **The message must never carry the value.** The connection string embeds a password (D-13).
   Compose the message from plain string literals concatenated together; do not use an
   interpolated string and do not include the local's value in the message or in any log call.
   This is the mitigation for threat `T-01-03` below.
3. **The guard is unconditional.** Do not wrap it in an environment check. Its value is failing
   identically and immediately everywhere, including on a misconfigured developer machine.

Follow the codebase's existing error idiom (PATTERNS.md, "plain-exception, no-custom-type"):
there is no custom configuration-exception type and no startup validation framework in use —
`MigrationExtensions.cs:56` simply rethrows. Do not introduce a new exception type, a new
extension method, or an options-validation package for this one guard.
  </action>

  <acceptance_criteria>
    **Source assertions** (region = from the `GetConnectionString` line through the `AddNpgsqlDbContext` line)
    - `grep -c 'GetConnectionString("okozukai")' src/Okozukai.Api/Program.cs` returns `1` — read exactly once, nowhere else in the file.
    - The `GetConnectionString("okozukai")` line number is **less than** the `builder.AddNpgsqlDbContext` line number.
    - `awk '/GetConnectionString\("okozukai"\)/,/AddNpgsqlDbContext/' src/Okozukai.Api/Program.cs | grep -c 'ConnectionStrings__okozukai'` returns `1` — the message names the key in its environment-variable spelling.
    - `awk '/GetConnectionString\("okozukai"\)/,/AddNpgsqlDbContext/' src/Okozukai.Api/Program.cs | grep -vE '^[[:space:]]*//' | grep -cF '$"'` returns `0` — no interpolated string in the guard region, so the secret-bearing value cannot reach the message (threat `T-01-03`).
    - `awk '/GetConnectionString\("okozukai"\)/,/AddNpgsqlDbContext/' src/Okozukai.Api/Program.cs | grep -c 'IsNullOrWhiteSpace'` returns `1` — whitespace-only is rejected, not just null/empty.
    - `awk '/GetConnectionString\("okozukai"\)/,/AddNpgsqlDbContext/' src/Okozukai.Api/Program.cs | grep -vE '^[[:space:]]*//' | grep -c 'IsDevelopment'` returns `0` — the guard is unconditional.
    - `git diff --name-only HEAD~1 HEAD` lists only `src/Okozukai.Api/Program.cs`.

    **Behaviour — misconfigured startup (PROD-05 empty edge, D-14)**
    - Starting with `ConnectionStrings__okozukai` **unset** exits non-zero.
    - Starting with `ConnectionStrings__okozukai` set to the **empty string** exits non-zero.
    - Starting with `ConnectionStrings__okozukai` set to **a single space** exits non-zero.
    - In all three cases the captured output contains the literal `ConnectionStrings__okozukai`.
    - In all three cases the captured output contains **zero** occurrences of `Attempting to apply database migrations` — the retry loop is never entered, proving the guard runs strictly earlier.

    **No regression**
    - `dotnet build Okozukai.slnx -nologo` exits 0 and `dotnet test Okozukai.slnx --no-build -nologo` exits 0 (the integration tests inject their own DbContext but still boot through `Program`, so a mis-placed guard would break all 22 of them).
    - Re-running Task 1's Production boot with a **valid** `ConnectionStrings__okozukai` still reaches `200` on `/health` — the guard does not reject good input.
  </acceptance_criteria>

  <verify>
    <automated>dotnet build Okozukai.slnx -nologo &amp;&amp; dotnet test Okozukai.slnx --no-build -nologo</automated>
    <automated>
set -u
# --- D-14: absent / empty / whitespace connection string must fail fast ----------
n=0
for VAL in __UNSET__ "" " "; do
  n=$((n+1)); LOG=/tmp/okoz-noconn-$n.log
  if [ "$VAL" = __UNSET__ ]; then
    env -u ConnectionStrings__okozukai ASPNETCORE_ENVIRONMENT=Production \
      ASPNETCORE_URLS=http://127.0.0.1:5099 \
      dotnet run --project src/Okozukai.Api --no-build --no-launch-profile >"$LOG" 2>&1
  else
    ASPNETCORE_ENVIRONMENT=Production ASPNETCORE_URLS=http://127.0.0.1:5099 \
      ConnectionStrings__okozukai="$VAL" \
      dotnet run --project src/Okozukai.Api --no-build --no-launch-profile >"$LOG" 2>&1
  fi
  rc=$?
  [ "$rc" -ne 0 ] || { echo "case $n: expected non-zero exit, got 0"; exit 1; }
  grep -q 'ConnectionStrings__okozukai' "$LOG" || { echo "case $n: message does not name the key"; exit 1; }
  test "$(grep -c 'Attempting to apply database migrations' "$LOG")" = 0 || { echo "case $n: retry loop was entered"; exit 1; }
done
# guard must not reject a good value: source-level ordering + single read
test "$(grep -c 'GetConnectionString("okozukai")' src/Okozukai.Api/Program.cs)" = 1 || exit 1
G=$(grep -n 'GetConnectionString("okozukai")' src/Okozukai.Api/Program.cs | cut -d: -f1)
A=$(grep -n 'AddNpgsqlDbContext' src/Okozukai.Api/Program.cs | cut -d: -f1)
[ "$G" -lt "$A" ] || { echo "guard is not before the DbContext registration"; exit 1; }
awk '/GetConnectionString\("okozukai"\)/,/AddNpgsqlDbContext/' src/Okozukai.Api/Program.cs \
  | grep -vE '^[[:space:]]*//' | grep -qF '$"' &amp;&amp; { echo "interpolated string in guard region"; exit 1; }
echo "FAIL-FAST GUARD VERIFICATION PASSED"
    </automated>
  </verify>

  <done>
Starting the API without a usable `ConnectionStrings__okozukai` value terminates immediately
with a message naming that exact environment variable, before the migration retry loop is
entered and without echoing the secret-bearing value; a valid value still boots normally.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit the deployment environment contract</name>

  <files>.env.example</files>

  <read_first>
    - `.gitignore` — read lines 66-80. This is the **scope-down check**: CONTEXT.md's D-16 says
      *"`.gitignore` currently has no `.env` entry — one must be added."* That claim is stale.
      The `# Environment Secrets` block already contains `.env` and four related patterns; it
      landed in commit `880a8eb`, after the CONTEXT.md session concluded. Confirm this by
      reading, then do **not** edit `.gitignore`.
    - `src/Okozukai.Api/Program.cs` — post-Task-2 state, lines 1-35. The variable names written
      into `.env.example` must be the ones the code actually reads.
    - `src/Okozukai.Api/appsettings.json` and `appsettings.Development.json` — confirm neither
      declares a `ConnectionStrings` section, which is why the environment is the only source.
    - `.planning/phases/01-production-readiness/01-PATTERNS.md` — the `.env.example` section.
      No `.env*` file exists anywhere in this repo, so there is no in-repo precedent; use the
      shape given there.
  </read_first>

  <action>
Create `.env.example` at the repository root (sibling to `.gitignore`, `README.md`, `LICENSE`
and `src/`) documenting every variable the API needs to start in Production, per D-16 and
PROD-05. Exactly three keys, each on its own line in `KEY=value` form so a compose `env_file`
can consume the copied file directly in Phase 2:

- `ASPNETCORE_ENVIRONMENT=Production` (D-08)
- `ASPNETCORE_URLS=http://+:8080` — plain HTTP only, no HTTPS endpoint, so no dev-cert or PFX
  plumbing is needed in a container (D-08). Port 8080 is the .NET container-image default since
  .NET 8, chosen under Claude's discretion; nothing depends on it until Phase 2 fixes the
  deployment contract.
- `ConnectionStrings__okozukai=Host=postgres;Port=5432;Database=okozukai;Username=okozukai;Password=changeme`
  — double underscore, which ASP.NET Core's environment-variable provider maps to the
  `ConnectionStrings:okozukai` configuration key (D-13).

Add a short header comment stating that this file is an example to be copied to `.env` and
filled in with real values, and that `.env` itself is already ignored by git. The committed
placeholder password must be an obvious non-secret (`changeme`); never commit a real
credential. Also note in the header that a password containing `;` or `=` must follow Npgsql's
keyword-value quoting rules — the example shows only the plain unquoted form, and the real
value is authored at deploy time.

**Scope note (do not silently drop this).** D-16 has two halves. The `.env.example` half is
this task. The `.gitignore` half is **already satisfied** and is deliberately scoped out: the
`# Environment Secrets` block in `.gitignore` already ignores `.env` plus four related
patterns. RESEARCH.md logs this as Assumption A2 / Open Question 1. Adding a duplicate pattern
would be a no-op that reports success without doing anything, muddying verification — so this
task **verifies** the existing ignore rule with `git check-ignore` instead of re-adding it.

Do not create `appsettings.Production.json`. Claude's discretion under D-16/CONTEXT.md leaves
this open, and the choice here is not to create one: `appsettings.json` already sets sane log
levels (`Default: Information`, `Microsoft.AspNetCore: Warning`), and adding an empty
environment-specific file would reintroduce a per-environment configuration surface for no
benefit. Record this choice in the SUMMARY.
  </action>

  <acceptance_criteria>
    - `test -f .env.example` succeeds and the file is at the repository root.
    - `grep -cE '^ASPNETCORE_ENVIRONMENT=Production$' .env.example` returns `1`.
    - `grep -cE '^ASPNETCORE_URLS=http://' .env.example` returns `1`.
    - `grep -cE '^ConnectionStrings__okozukai=Host=' .env.example` returns `1` (double underscore, matching what `Program.cs` reads via `GetConnectionString("okozukai")`).
    - `grep -c 'changeme' .env.example` returns `1` — the committed password is an obvious placeholder.
    - `git ls-files --error-unmatch .env.example` exits `0` — the example **is** tracked.
    - `git check-ignore -v .env` exits `0` and prints the matching `.gitignore` rule — the real file **is** ignored (this is the traceability checkbox for D-16's scoped-out half; no `.gitignore` edit is made).
    - `git ls-files --error-unmatch .env` exits **non-zero** — no real `.env` has been committed.
    - `git diff --name-only HEAD -- .gitignore` prints nothing — `.gitignore` was not modified.
    - `test -f src/Okozukai.Api/appsettings.Production.json` fails — no Production settings file was created.
    - Every key in `.env.example` is one the code actually consumes: `grep -c 'GetConnectionString("okozukai")' src/Okozukai.Api/Program.cs` returns `1`, and `grep -c 'UserSecretsId' src/Okozukai.Api/Okozukai.Api.csproj` returns `0` (the API project loads no developer secret store in any environment, so the environment is the only source — PROD-05 adjacency edge).
  </acceptance_criteria>

  <verify>
    <automated>
set -u
test -f .env.example || exit 1
test "$(grep -cE '^ASPNETCORE_ENVIRONMENT=Production$' .env.example)" = 1 || exit 1
test "$(grep -cE '^ASPNETCORE_URLS=http://' .env.example)" = 1 || exit 1
test "$(grep -cE '^ConnectionStrings__okozukai=Host=' .env.example)" = 1 || exit 1
test "$(grep -c 'changeme' .env.example)" = 1 || exit 1
git check-ignore -v .env || { echo ".env is NOT gitignored"; exit 1; }
git ls-files --error-unmatch .env 2>/dev/null &amp;&amp; { echo "a real .env is tracked"; exit 1; }
test -z "$(git diff --name-only HEAD -- .gitignore)" || { echo ".gitignore was modified"; exit 1; }
test ! -f src/Okozukai.Api/appsettings.Production.json || exit 1
test "$(grep -c 'GetConnectionString("okozukai")' src/Okozukai.Api/Program.cs)" = 1 || exit 1
test "$(grep -c 'UserSecretsId' src/Okozukai.Api/Okozukai.Api.csproj)" = 0 || exit 1
echo "ENV CONTRACT VERIFICATION PASSED"
    </automated>
  </verify>

  <done>
`.env.example` is committed at the repository root naming exactly the three variables the API
reads at startup, the real `.env` is confirmed ignored by the pre-existing `.gitignore` rule
without a redundant edit, and no `appsettings.Production.json` was introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process environment → API host builder | The secret-bearing connection string crosses here as `ConnectionStrings__okozukai`. Whoever can set the process environment (or read it back via `docker inspect`) controls and can read the database credential. |
| container network → Kestrel HTTP listener | Unauthenticated `/health`, `/alive` and `/api/*` become reachable in Production for the first time. Plain HTTP; there is no TLS terminator anywhere in this milestone's topology (D-07). |
| API process → PostgreSQL | Migrations now execute automatically at every startup, mutating the physical schema of the ledger database with whatever privileges the app role holds (including `CREATEDB` for the `3D000` fallback). |

## STRIDE Threat Register

ASVS level 1 · blocking threshold: **high**.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `ConnectionStrings__okozukai` process environment variable | medium | accept | Accepted trade-off recorded in D-13 for a single-tenant, Tailnet-only box: the password is visible to anyone who can run `docker inspect` or read `/proc`. Discrete `POSTGRES_*` variables and the Docker-secret `_FILE` pattern were both considered and rejected as disproportionate. Revisit if the box ever becomes multi-tenant. |
| T-01-02 | Information Disclosure | `/health` and `/alive`, newly mapped outside Development (`Extensions.cs` `MapDefaultEndpoints`) | low | transfer | Transferred to the network layer rather than mitigated in app code, per D-11: Phase 3's ACC-03 never publishes the API port to the host, so the endpoints exist only on the internal container network. Microsoft's stock advice (auth, host filtering, caching) is deliberately substituted with topology isolation. The default health-check response writer emits only the status word, no exception text. **Forward constraint for Phase 3:** if ACC-03 slips, this becomes medium and needs `RequireHost` or auth. |
| T-01-03 | Information Disclosure | D-14 fail-fast guard in `Program.cs` — startup error message and log | high | mitigate | The guard's message is composed from plain string literals and names only the *key* `ConnectionStrings__okozukai`, never the value. Enforced by a Task 2 acceptance criterion that greps the guard region for interpolated strings and requires zero. Without this, the first thing an operator pastes into a bug report is the database password. |
| T-01-04 | Spoofing | Forwarded-header trust (`X-Forwarded-Proto` and friends) | medium | mitigate | Mitigated by omission this phase: no forwarded-header middleware is added (D-07), and Task 1 asserts its absence, so there is no header to forge trust from. **Forward constraint for Phase 2/3:** if a TLS terminator is ever introduced, `KnownProxies`/`KnownNetworks` must be configured explicitly — the defaults trust `X-Forwarded-*` from any source. |
| T-01-05 | Denial of Service (self-inflicted) | Migration retry loop, `MigrationExtensions.cs:18-58` | low | mitigate | A missing connection string previously produced ten rounds of 3-second backoff and opaque Npgsql errors before the real cause surfaced — thirty seconds of misleading boot. Task 2's guard runs before the loop is entered; the acceptance criteria assert the loop's log line never appears in the three misconfiguration cases. |
| T-01-06 | Tampering / Elevation of Privilege | Unconditional `ApplyDatabaseMigrations()` against the production database, incl. the `3D000` auto-create path | medium | accept | Accepted per D-01/D-02/D-03: single instance is a locked milestone constraint, migration failure crashes the process rather than serving a half-migrated ledger, and the auto-create fallback is a safety net for first boot. The app role needs `CREATEDB` for that fallback — **carried forward to Phase 2** as a Postgres provisioning note (see Carried-Forward Notes); if the role lacks it, the fallback logs a creation failure and the boot crash-loops instead. |
| T-01-SC | Tampering | Package-manager installs (NuGet) | low | accept | This plan adds **zero** package references. RESEARCH.md's Package Legitimacy Audit records "Not applicable this phase", and D-10 explicitly forbids adding a health-check package because `AddNpgsqlDbContext` already registers one. Task 1 asserts `git diff` shows no `.csproj` modified. Re-run the Package Legitimacy Gate if any revision introduces an install. |

No threat is dispositioned `accept` at severity ≥ high, so the `security_block_on: high` gate is
satisfied.
</threat_model>

<verification>
Phase-level checks, run after all three tasks are committed:

1. `dotnet build Okozukai.slnx -nologo` exits 0.
2. `dotnet test Okozukai.slnx --no-build -nologo` exits 0 — the 22 integration tests and the
   unit tests still pass. (These run in Development against the in-memory provider and are
   structurally incapable of exercising the Production path — see Known Limits below.)
3. Task 1's Production boot block passes end to end, printing
   `PRODUCTION BOOT VERIFICATION PASSED`.
4. Task 2's fail-fast block passes, printing `FAIL-FAST GUARD VERIFICATION PASSED`.
5. Task 3's contract block passes, printing `ENV CONTRACT VERIFICATION PASSED`.
6. `git status --short` shows a clean tree apart from planning artifacts.

**Known limits of automated coverage (D-05).** `CustomWebApplicationFactory` calls
`builder.UseEnvironment("Development")` unconditionally
(`tests/Okozukai.IntegrationTests/CustomWebApplicationFactory.cs:37`) and swaps in
`UseInMemoryDatabase`, for which `Database.IsRelational()` is false and
`ApplyDatabaseMigrations` takes its early-skip branch. The existing suite therefore cannot
observe whether the migration call sits inside or outside an environment guard, in either
direction. Do **not** add a test claiming to cover the Production migration path: it would
either be a silent no-op or require Testcontainers, which CONTEXT.md rules out as
disproportionate for a configuration phase. The real Production boot in Task 1 is the
verification.
</verification>

<flagged_assumptions>
Surfaced explicitly rather than dropped. Each needs a human read before the phase seals.

1. **PROD-02 edge probe returned `unclassified`** — the deterministic probe applied no shape
   category to PROD-02 and emitted `unclassified — review manually`. Per the spec-less fallback
   protocol an `unclassified` row stays `unresolved` and is **never** auto-resolved with a
   backstop marker, so it is recorded here rather than in `must_haves.truths`.

   *Manual review, for the human's benefit — not an auto-resolution:* the operative edge for
   PROD-02 is **what counts as "Development"**. `IHostEnvironment.IsDevelopment()` matches the
   literal name `Development` case-insensitively, so `production`, `Staging`, `Prod`, a typo'd
   value, and an entirely unset `ASPNETCORE_ENVIRONMENT` (which defaults to `Production`) all
   correctly skip the seed — but a lowercase `development` **does** seed. The sharper risk is
   not the environment name at all: `DevSeedData.SeedDevelopmentData` calls `RemoveRange` on
   every tag and every journal before writing (`DevSeedData.cs:36-38`), guarded only by
   "are there zero transactions?". A real database that holds journals and tags but no
   transactions would be **wiped** by an accidental Development start. That risk is unchanged
   by this phase and is not in scope to fix here, but a human should decide whether it warrants
   a follow-up requirement.

2. **`CREATEDB` on the eventual homelab Postgres role is unverifiable in Phase 1** — D-03 keeps
   the `3D000` auto-create fallback active in Production and assumes the app's role holds
   `CREATEDB`. Task 1 proves the fallback works against the *local* Homebrew Postgres, where the
   developer's own role is a superuser. The homelab role is provisioned in Phase 2 (compose
   `POSTGRES_USER`/`POSTGRES_DB`), so nothing in Phase 1 can confirm it. Carried forward, not
   closed. See Carried-Forward Notes.

3. **`ASPNETCORE_URLS` port 8080 is Claude's discretion, not a locked decision** — CONTEXT.md
   leaves the port open and notes nothing depends on it until Phase 2 fixes the deployment
   contract. 8080 was chosen as the .NET container-image default since .NET 8. Phase 2 may
   change it freely; only `.env.example` records it today.
</flagged_assumptions>

<carried_forward_notes>
Recorded for Phase 2 / Phase 3 planning. Not action items for this plan.

- **Postgres role privileges (from T-01-06 / D-03).** The API's PostgreSQL role must either hold
  `CREATEDB`, or the deployment must rely exclusively on the Postgres container's `POSTGRES_DB`
  auto-creation on first boot — in which case D-03's fallback is purely defensive and may not
  actually work when needed. Decide this when provisioning the compose Postgres service.
- **Health-check contract for Phase 2 (D-12).** `/health` = ready (includes the PostgreSQL
  connectivity check); `/alive` = live (process `self` check only). Phase 2 owns the Dockerfile
  `HEALTHCHECK` and any `depends_on: condition: service_healthy`; Phase 1 only exposes the
  endpoints.
- **Forwarded headers if TLS is ever introduced (from T-01-04 / D-07).** Adding a TLS terminator
  means revisiting Phase 2's entry point and Phase 3's Tailnet wiring, and requires explicitly
  configuring `KnownProxies`/`KnownNetworks` — the defaults trust forwarded headers from any
  source.
- **`.env` consumption (D-16).** `.env.example` exists so Phase 2's compose file can consume a
  copied `.env` via `env_file`, satisfying PKG-05's "not committed to the repository".
- **Aspire AppHost stays dev-only (D-15).** `src/Okozukai.AppHost/Program.cs` keeps its
  hardcoded `ASPNETCORE_ENVIRONMENT=Development` and its user-secrets connection string.
  Compose is the production runner; `aspire run` remains the local development experience.
</carried_forward_notes>

<success_criteria>
Measured against the ROADMAP's five success criteria for Phase 1:

1. **SC-1 (PROD-01)** — A Production start against a brand-new empty database applies all six
   migrations and yields a working schema; a start against a database that does not exist yet
   creates it first. Proven by Task 1's boot block (`__EFMigrationsHistory` = 6 rows in both
   the empty-database and missing-database cases).
2. **SC-2 (PROD-02)** — No seed data in Production: `Journals`, `Tags` and `Transactions` all
   return `count(*) = 0` after a Production boot, and the seed log line never appears.
3. **SC-3 (PROD-03)** — No redirect loop: `/api/journals` over plain HTTP returns `200` with an
   empty `redirect_url`, single and under ten-way concurrency. Note the *reason* this holds is
   topological, not the textbook fix — D-07 locks in a topology with no TLS terminator, so
   removing the redirect middleware is correct and `UseForwardedHeaders` has nothing to
   configure; see `T-01-04` for what changes if TLS is ever introduced.
4. **SC-4 (PROD-04)** — `/health` and `/alive` both return `200 Healthy` under
   `ASPNETCORE_ENVIRONMENT=Production`. "Internal port" is satisfied by topology (D-11), not by
   app code: Phase 3's ACC-03 never publishes the API port to the host.
5. **SC-5 (PROD-05)** — The API starts with configuration supplied purely as process
   environment variables, and misconfiguration fails fast and legibly. The API project declares
   no user-secrets identifier at all, so no developer secret store is consulted in any
   environment.

Plus: no `.csproj`, `MigrationExtensions.cs`, `DevSeedData.cs`, AppHost, or `.gitignore`
change; the full test suite still passes.
</success_criteria>

<output>
Create `.planning/phases/01-production-readiness/01-01-SUMMARY.md` when done.

Record in it: the captured evidence lines from the three verification blocks; the discretionary
choices made (port `8080`; guard placed inline in `Program.cs` rather than as an extension
method; `appsettings.Production.json` deliberately not created); the confirmation that
`.gitignore` needed no edit and why; and the flagged assumptions and carried-forward notes
above, unchanged, for Phase 2's planner.
</output>
