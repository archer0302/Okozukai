---
context: phase
phase: 02-single-origin-packaging
task: 0
total_tasks: 7
status: not_started
last_updated: 2026-08-22T00:59:31.672Z
---

# Handoff — Phase 2 paused for a machine move

> This is **not** a mid-task interruption. Phase 2 is fully planned and **zero tasks have been
> executed**. No source file under `src/` has been modified for this phase. The working tree
> carries no code changes at all.
>
> The reason for the pause is that the session is relocating to a different machine. The only
> thing that does not survive that move is the conversation transcript; everything that matters
> is under `.planning/` and tracked in git.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting `02-CONTEXT.md` on `apiPort` | Context said `apiPort` / `TAILNET_API_PORT` could be removed from the AppHost. It cannot — the API's own `ASPNETCORE_URLS` wiring still consumes them. | advisory | `02-PATTERNS.md` carries the correction and `02-02-PLAN.md` encodes it as a must-have. Read PATTERNS before CONTEXT where the two disagree. |
| Re-litigating the API-coverage gate | The coverage detector fires six signals on the Phase 2 plans; all six are the word "API" referring to this repo's own `src/Okozukai.Api`. | advisory | `COVERAGE.md` documents each false positive row by row. Re-run the gate only if a revision adds a real external service client. |
| Reintroducing an absolute API base URL "just for dev" | Any absolute origin in the frontend's API layer can be baked into a production bundle. | advisory | `02-02-PLAN.md` carries this as an explicit prohibition; dev parity comes from a Vite proxy, never a base URL. |
| Volume-destroying remedies | The homelab disk is the only copy of the ledger — backups are deferred to v2. | advisory | `02-03-PLAN.md` prohibits any `down -v`-style step appearing in normal operation or committed docs, including as a fix for a credential mismatch. |

<current_state>
Phase 1 (Production Readiness) is verified and complete: 16/18 must-haves, 0 open threats, UAT
2/2. Both `human_verification` items in `01-VERIFICATION.md` have now been answered, so its status
is flipped from `human_needed` to `passed` (committed with this handoff).

Phase 2 (Single-Origin Packaging) has been through context capture, research, pattern mapping and
planning. Three plans exist across two waves. Nothing has been executed.
</current_state>

<completed_work>

- Phase 2 context captured — `02-CONTEXT.md`, `02-DISCUSSION-LOG.md` (commit `e77dd15`)
- Phase 2 domain research — `02-RESEARCH.md` (commit `e758705`)
- Phase 2 pattern mapping — `02-PATTERNS.md` (was untracked; committed with this handoff)
- Phase 2 planning — `02-01-PLAN.md`, `02-02-PLAN.md`, `02-03-PLAN.md`, `COVERAGE.md`
  (commits `af32c23`, `eaa151e`)
</completed_work>

<remaining_work>

All 7 tasks across 3 plans, in 2 waves:

**Wave 1**
- `02-01` (2 tasks) — Origin collapse and container packaging. Strip CORS from
  `src/Okozukai.Api/Program.cs`, make the axios base URL relative, author `Dockerfile`,
  `.dockerignore`, `compose.yaml`, `.env.example`, `.nvmrc`.

**Wave 2** (both depend on `02-01`, disjoint files, parallelisable)
- `02-02` (3 tasks) — Dev-loop single-origin parity. Vite dev proxy, AppHost stops injecting a
  frontend API URL, and `AGENTS.md` / frontend README / codebase map stop instructing a reader to
  configure one.
- `02-03` (2 tasks) — Persistence and credential correctness. Named volume at exactly
  `/var/lib/postgresql/data`, `POSTGRES_DB` and the connection string made to agree so the `3D000`
  fallback never fires, and Npgsql quoting for a password containing `;` and `=`.
</remaining_work>

<decisions_made>

- Phase 2 split into 3 plans / 2 waves — `02-01` establishes the origin collapse and compose stack
  that the other two build on; afterwards the dev-loop and persistence work touch disjoint files.
- `apiPort` / `TAILNET_API_PORT` survive in the AppHost — only the *frontend* API-URL injection is
  removed. `02-PATTERNS.md` corrects `02-CONTEXT.md` here.
- Development is made single-origin via a Vite dev proxy, not a dev-only absolute base URL (D-06).
- `COVERAGE.md` declares no external API integration; the detector's six hits are all false
  positives on this repository's own API project.
</decisions_made>

<blockers>
- **`CREATEDB` on the app's PostgreSQL role** — required for the `3D000` auto-create fallback, and
  unverifiable from Phase 1's local Homebrew Postgres where the role is already superuser.
  `02-03` sidesteps it by making the two configuration surfaces agree so the fallback never fires.
- **Two Phase 1 UAT items deferred to deploy-time here** — an interrupted mid-migration startup
  resuming from `__EFMigrationsHistory` (`02-01`), and an Npgsql password containing `;` or `=`
  (`02-03`). Both are now must-haves in those plans.
- **Docker is a hard prerequisite** on whatever machine executes this phase.
</blockers>

## Required Reading (in order)

1. `.planning/phases/02-single-origin-packaging/02-01-PLAN.md` — wave 1, the only plan that can
   start immediately
2. `.planning/phases/02-single-origin-packaging/02-PATTERNS.md` — closest-analog mapping for every
   new file, and the `apiPort` correction to `02-CONTEXT.md`
3. `.planning/phases/02-single-origin-packaging/02-RESEARCH.md` — four numbered pitfalls that the
   plans' must-haves are written against
4. `.planning/phases/02-single-origin-packaging/COVERAGE.md` — why the API-coverage gate is
   declared satisfied; read before re-running it
5. `.planning/STATE.md` — accumulated decisions and the carried blocker list

## Infrastructure State

- **Nothing is running.** No dev server, no `aspire run`, no compose stack, no watcher.
- No async external jobs — `.planning/async-jobs/` does not exist.
- Test baselines the plans use as regression guards: 31 unit tests, 22 WebApplicationFactory
  integration tests, 16 Vitest component tests, 14 Playwright E2E tests in `e2e/dashboard.spec.ts`.

## Machine Move — what does and does not travel

**Travels (tracked in git):** all of `.planning/` — `PROJECT.md`, `ROADMAP.md`, `STATE.md`,
`REQUIREMENTS.md`, `codebase/`, both phase directories, and `HANDOFF.json`.

**Does not travel (`.claude/` is gitignored at `.gitignore:82`):** the whole GSD install —
`gsd-core/`, `commands/`, `agents/`, `hooks/`, `gsd-install-state.json`, `settings.local.json`.
Reinstall GSD on the new machine to regenerate it. Do **not** copy `settings.local.json` across:
it hardcodes `/Users/archer.chang/.nvm/versions/node/v22.18.0/bin/node`, which will differ.

**Deliberately left untracked:** `.gsd/` (runtime dispatch sentinel) and `.planning/research/.cache/`
(5 files, 20K of regenerable web-fetch cache).

**Setup checklist on the new machine:**
1. Clone the repo.
2. Install Claude Code, then run the GSD installer in the repo root to recreate `.claude/`.
3. Re-enable the `aspire` and `playwright` MCP servers.
4. Install Docker (`docker compose version` must work), the .NET 10 SDK, and Node 22.
5. `/gsd-resume-work`.

<context>
The phase was planned to completion in the previous session and stopped cleanly at the planning
boundary — the natural place to break. There is no half-finished edit anywhere and no mental state
worth reconstructing beyond what the plans already encode. The plans are unusually detailed
(55K + 36K + 36K of plan text) precisely because the execution is deploy-shaped and hard to verify
incrementally; trust them over any recollection.
</context>

<next_action>
Start with: read `02-01-PLAN.md`, confirm Docker is available on this machine, then run
`/gsd-execute-phase 2`. Wave 1 is plan `02-01` alone.
</next_action>
